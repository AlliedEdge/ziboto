# Docker Compose Environment Variables

# JWT Secret (Generate with: openssl rand -base64 64)
JWT_SECRET=HJYuXxKYFT4euP13F4LSLpOy04amKn02lRdtlM4eDr2SmdPa6Sd7rlooTOzeBqDVRobztLHBBhrazss0kzXerA==

# AWS Configuration (for local development)
AWS_REGION=eu-north-1
AWS_S3_BUCKET=ziboto-files-277522752099-eu-north-1-an

# AWS Credentials (DO NOT USE IN PRODUCTION - use IAM roles)
# Uncomment for local testing only:
# AWS_ACCESS_KEY_ID=your_access_key_id
# AWS_SECRET_ACCESS_KEY=your_secret_access_key

# Resend Email Configuration
RESEND_API_KEY=your_resend_api_key_here
RESEND_FROM_EMAIL=no-reply@alliededge.app

# Frontend Configuration
VITE_API_URL=http://localhost:8080/api/v1

# Database (optional - defaults are in docker-compose.yml)
# POSTGRES_PASSWORD=change-this-in-production

# Redis (optional - defaults are in docker-compose.yml)
# REDIS_PASSWORD=change-this-in-production
